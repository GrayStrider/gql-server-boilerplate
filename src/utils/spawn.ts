import {spawn} from 'child_process'
import {log} from './log'

// const child = spawn('cmd', [ "/c", "ls"])
// const child = spawn('cmd', [ "/c", "yarn list axios"])
// const child = spawn('cmd', [ "/c", "yarn start"])
// child.stdout.on('data', (data) => log.debug(data.toString()))
