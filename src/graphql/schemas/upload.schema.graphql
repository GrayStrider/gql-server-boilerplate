scalar Upload
type File {
    filename: String!

    mimetype: String!
    encoding: String!
}
type Query {
    uploads: [File]
    files: [File]
}
type Mutation {
    singleUpload(file: Upload!): File!
}
